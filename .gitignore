# Deciduous — local binary DB only; JSON exports in docs/ are tracked
.deciduous/

# Rust build artifacts
/target/

# Environment secrets (but NOT .env.example — that's the template)
/.env

# SQLite database files
*.db
*.db-wal
*.db-shm

# Generated reports
/output/

# Database and report backups
/backups/

# Research scratch files
/docs/research/

# Chainlink local state — DB and cache are local; export JSON is tracked
.chainlink/issues.db
.chainlink/.cache/
# issues-export.json is explicitly tracked (see pre-commit hook)

# Git worktrees (local ephemeral state)
.worktrees/
.claude/worktrees/
