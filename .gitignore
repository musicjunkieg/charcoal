# ── Environment & secrets ──────────────────────────────────────────
.env
.env.local
.env.*.local

# ── macOS ──────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
.fseventsd
.apdisk

# ── Editors / IDEs ─────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~

# ── Rust ───────────────────────────────────────────────────────────
target/
Cargo.lock.bak
**/*.rs.bk

# ── Node / JavaScript / TypeScript ─────────────────────────────────
node_modules/
dist/
build/
.next/
.nuxt/
.svelte-kit/
.vite/
.turbo/
.parcel-cache/
*.tsbuildinfo
.npm/

# ── Python ─────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
.Python
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── Logs ───────────────────────────────────────────────────────────
*.log
logs/

# ── Claude Code per-clone state ────────────────────────────────────
.claude/settings.local.json

# ── Deciduous local DB ─────────────────────────────────────────────
# (deciduous.db is backed up via pre-commit to S3, not tracked in git)
.deciduous/deciduous.db
.deciduous/deciduous.db-shm
.deciduous/deciduous.db-wal

# ── Chainlink local state ──────────────────────────────────────────
# (issues.db is backed up via pre-commit to S3, not tracked in git)
# (issues-export.json is force-added by the pre-commit hook)
.chainlink/issues.db
.chainlink/issues.db-shm
.chainlink/issues.db-wal
.chainlink/.cache/
.chainlink/issues-export.json

# --- Charcoal-specific ---

# Local credential files (see .env.example for how to provide them)
refs/zentropi_info.txt

# Generated reports
/output/

# Database and report backups
/backups/

# Research scratch files
/docs/research/

# Broad SQLite catches (template's specific paths cover known dbs;
# this catches any ad-hoc db elsewhere)
*.db
*.db-wal
*.db-shm

# Broad deciduous catch (template covers specific files; this catches
# any other artifacts under .deciduous/)
.deciduous/
