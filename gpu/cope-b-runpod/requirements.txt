# Runtime
# 0.23.0 patches GHSA-q8gq-377p-jq3r + GHSA-5jv2-g5wq-cmr4 (present in 0.20.2);
# must match the vllm/vllm-openai base-image tag in the Dockerfile.
vllm==0.23.0
transformers>=4.50,<5
runpod>=1.7,<2
# Dev / test (installed locally for pytest runs; not in image)
pytest>=8
pytest-asyncio>=0.24
